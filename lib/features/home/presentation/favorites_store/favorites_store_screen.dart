import 'package:flutter/material.dart';

class FavoritesStoreScreen extends StatelessWidget {
  static const path = '/favorites-store';
  const FavoritesStoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('favoritesPage')],
    );
  }
}
