import 'package:flutter/material.dart';

class DiscoverStoreScreen extends StatelessWidget {
  static const path = '/discover-store';
  const DiscoverStoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('DiscoverPage')],
    );
  }
}
