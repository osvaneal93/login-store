import 'package:flutter/material.dart';

class AccountStoreScreen extends StatelessWidget {
  static const path = '/account-store';
  const AccountStoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('AccountPage')],
    );
  }
}
